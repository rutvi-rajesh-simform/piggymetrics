spring:
  application:
    name: notification-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://${CONFIG_SERVER_HOST:config-server}.${CLOUD_MAP_NAMESPACE:local}:8888}
      fail-fast: true
      username: ${CONFIG_SERVICE_USERNAME:user}
      password: ${CONFIG_SERVICE_PASSWORD}