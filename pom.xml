<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.piggymetrics</groupId>
    <artifactId>piggymetrics</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>piggymetrics</name>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.0.3.RELEASE</version>
        <relativePath/>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-cloud.version>Finchley.RELEASE</spring-cloud.version>
        <spring-cloud-aws.version>2.4.4</spring-cloud-aws.version>
        <aws.sdk.v2.version>2.25.57</aws.sdk.v2.version>
        <java.version>1.8</java.version>

        <!-- Cloud Map-friendly service URL defaults (override via env vars per environment) -->
        <auth.service.url>${AUTH_SERVICE_URL:http://auth-service}</auth.service.url>
        <account.service.url>${ACCOUNT_SERVICE_URL:http://account-service}</account.service.url>
        <statistics.service.url>${STATISTICS_SERVICE_URL:http://statistics-service}</statistics.service.url>
        <notification.service.url>${NOTIFICATION_SERVICE_URL:http://notification-service}</notification.service.url>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- AWS SDK v2 BOM for consistent SDK versions across modules -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.v2.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud AWS dependency alignment -->
            <dependency>
                <groupId>io.awspring.cloud</groupId>
                <artifactId>spring-cloud-aws-dependencies</artifactId>
                <version>${spring-cloud-aws.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Shared dependency management entry for Secrets Manager integration -->
            <dependency>
                <groupId>io.awspring.cloud</groupId>
                <artifactId>spring-cloud-starter-aws-secrets-manager-config</artifactId>
                <version>${spring-cloud-aws.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>config</module>
        <module>monitoring</module>
        <module>gateway</module>
        <module>auth-service</module>
        <module>account-service</module>
        <module>statistics-service</module>
        <module>notification-service</module>
        <module>turbine-stream-service</module>
    </modules>

    <!-- Keep Eureka registry available only when explicitly needed -->
    <profiles>
        <profile>
            <id>with-eureka-registry</id>
            <modules>
                <module>registry</module>
            </modules>
        </profile>
    </profiles>

</project>