spring:
  application:
    name: statistics-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://${CONFIG_SERVER_HOST:config-service}.${CLOUD_MAP_NAMESPACE:local}:${CONFIG_SERVER_PORT:8888}}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user