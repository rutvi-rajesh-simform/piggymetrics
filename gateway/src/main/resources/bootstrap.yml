spring:
  application:
    name: gateway
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://${CONFIG_SERVER_HOST:config-server}.${INTERNAL_DNS_ZONE:local}:${CONFIG_SERVER_PORT:8888}}
      fail-fast: true
      username: ${CONFIG_SERVICE_USERNAME:user}
      password: ${CONFIG_SERVICE_PASSWORD}